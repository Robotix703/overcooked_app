import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.robotix703.overcooked',
  appName: 'overcooked_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
